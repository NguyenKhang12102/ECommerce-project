# ECommerce-project

## Frontend

```bash
cd frontend
npm install
npm run dev
```

## Backend

```bash
cd backend
mvn spring-boot:run
```

Backend mặc định chạy ở `http://localhost:8080` và có endpoint `GET /api/products`.